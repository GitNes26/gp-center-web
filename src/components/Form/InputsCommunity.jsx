/**
 * Estos Inputs, deben de estar dentro de Formik, validados con Yup y dentro de grillas
 * @param {*} param0
 * @returns
 */
const InputsCommunity = ({ idNam }) => {
   return "ja";
};

export default InputsCommunity;
